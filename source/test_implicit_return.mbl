# Test implicit return (data scope)
build_data():
	title = "Test Report"
	count = 42
	status = "complete"

# Get the record
data = build_data()
program.write("Got data record")

# Try property access
program.write("Title: " + data.title)
program.write("Count: " + data.count)

program.write("Hello.  I have questions.")
name = program.prompt("What is your name? ")
born_text = program.prompt("What is your birth year? ")

# Let's use hardcoded conversion for now
born = 1970  # Simulating conversion of born_text
age = get_age(2025, born)
program.write("You must be " + age + " years old now.")

get_age(year, born):
	result = year - born
	return result

program.write("Good bye.")